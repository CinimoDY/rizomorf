<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="baseUrl" content="{{ baseUrl }}">
    <title>{{ title }} - DOS Mode</title>
    <link rel="stylesheet" href="{{ baseUrl }}/css/style.css">
    <script defer src="{{ baseUrl }}/js/title.js"></script>
    <script defer src="{{ baseUrl }}/js/search.js"></script>
    <script defer src="{{ baseUrl }}/js/dos-prompt.js"></script>
    <style>
        /* Main layout */
        body {
            min-height: 100vh;
            margin: 0;
            padding: 2px; /* Creates the frame */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            background: var(--dos-black);
        }

        /* Main content wrapper */
        .content-wrapper {
            padding: 0 2rem;
            margin-top: calc(var(--title-height) + 1rem);
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        /* Sticky navigation */
        nav {
            position: fixed;
            top: 4px; /* Increased to prevent overlap */
            left: 4px; /* Aligned with frame */
            right: 4px; /* Aligned with frame */
            background: var(--dos-black);
            padding: 0.5rem 2rem;
            z-index: 1001;
            display: flex;
            gap: 1rem;
        }

        nav a {
            color: var(--dos-yellow);
            text-decoration: none;
            padding: 0.25rem 0.5rem;
        }

        nav a:hover {
            background: var(--dos-yellow);
            color: var(--dos-black);
        }

        /* DOS prompt - stays within frame */
        #dos-prompt {
            background: var(--dos-black);
            padding: 1rem 2rem;
            margin-top: auto; /* Push to bottom of content */
            margin-bottom: 2px; /* Align with frame */
        }

        /* Window controls */
        .window-controls {
            position: fixed;
            top: 4px; /* Aligned with nav */
            right: 4px;
            padding: 0.5rem 2rem;
            color: var(--dos-yellow);
            display: flex;
            align-items: center;
            font-weight: bold;
            z-index: 1001;
        }

        /* Remove sticky footer since newsletter moved to index */
        .sticky-footer {
            display: none;
        }

        /* DOS-style form customization */
        .formkit-form {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: none !important;
            font-family: 'DOS', monospace !important;
            padding: 1rem !important;
            margin: 0 !important;
        }
        .formkit-form * {
            font-family: 'DOS', monospace !important;
        }
        .formkit-form button,
        .formkit-form [data-element="submit"] {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
            font-family: 'DOS', monospace !important;
            text-transform: uppercase !important;
        }
        .formkit-form button:hover,
        .formkit-form [data-element="submit"]:hover {
            background: var(--dos-yellow) !important;
            color: var(--dos-black) !important;
        }
        .formkit-form input,
        .formkit-form [data-element="field"] {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
            font-family: 'DOS', monospace !important;
        }
        .formkit-form .formkit-alert {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
        }
        .formkit-form .formkit-guarantee {
            color: var(--dos-yellow) !important;
        }
        #convertkit-form-container {
            max-width: 640px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        /* Mobile navigation adjustments */
        @media (max-width: 600px) {
            .content-wrapper {
                padding: 0 1rem;
            }

            nav {
                display: none;
                padding: 0.5rem 1rem;
            }

            nav.show {
                display: flex;
                flex-direction: column;
            }

            #dos-prompt {
                padding: 1rem;
            }

            .window-controls {
                padding: 0.5rem 1rem;
            }
        }
    </style>
    <script>
        // Force scroll to top on page load and navigation
        function scrollToTop() {
            window.scrollTo({
                top: 0,
                left: 0,
                behavior: 'instant'
            });
        }
        
        // Run on initial load
        window.onload = scrollToTop;
        
        // Run on DOM content loaded
        document.addEventListener('DOMContentLoaded', scrollToTop);
        
        // Run on history changes (navigation)
        window.addEventListener('popstate', scrollToTop);
        
        // Run on hash changes
        window.addEventListener('hashchange', scrollToTop);

        // Toggle mobile navigation
        document.addEventListener('DOMContentLoaded', function() {
            const menuToggle = document.querySelector('.menu-toggle');
            const nav = document.querySelector('nav');
            
            menuToggle?.addEventListener('click', function(e) {
                e.preventDefault();
                nav.classList.toggle('show');
            });
        });
    </script>
</head>
<body data-page-title="{{ title }}">
    <div class="window-controls">[guest]</div>
    <div class="scanlines"></div>
    
    <nav role="navigation" aria-label="Main navigation">
        <a href="{{ baseUrl }}/blog/">RECENTS</a>
        <a href="{{ baseUrl }}/ux-work/">UX WORK</a>
        <a href="{{ baseUrl }}/about.html">ABOUT</a>
        <a href="{{ baseUrl }}/faq.html">FAQ</a>
        <a href="{{ baseUrl }}/">INDEX</a>
    </nav>

    <div class="content-wrapper">
        {% block content %}
        {{ content | safe }}
        {% endblock %}
    </div>

    <!-- DOS Prompt -->
    <div id="dos-prompt"></div>
</body>
</html> 
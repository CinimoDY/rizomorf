<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="baseUrl" content="{{ baseUrl }}">
    <title>{{ title }} - DOS Mode</title>
    <link rel="stylesheet" href="{{ baseUrl }}/css/style.css">
    <script defer src="{{ baseUrl }}/js/title.js"></script>
    <script defer src="{{ baseUrl }}/js/search.js"></script>
    <script defer src="{{ baseUrl }}/js/dos-prompt.js"></script>
    <style>
        /* Main content wrapper */
        .content-wrapper {
            min-height: calc(100vh - 100px);
            padding: 0 2rem 4rem 2rem; /* Added horizontal padding and increased bottom padding */
            margin-top: calc(var(--title-height) + var(--min-touch-target));
        }

        /* Sticky navigation */
        nav {
            position: fixed;
            top: var(--title-height);
            left: 2rem; /* Match content padding */
            right: 2rem; /* Match content padding */
            background: var(--dos-black);
            padding: 0.5rem 1rem;
            z-index: 1001;
            border-bottom: 2px solid var(--dos-yellow);
        }

        /* Sticky DOS prompt */
        #dos-prompt {
            position: relative; /* Changed to relative */
            background: var(--dos-black);
            border-top: 2px solid var(--dos-yellow);
            padding: 1rem 2rem; /* Match content padding */
            margin-top: 2rem;
        }

        /* Remove sticky footer since newsletter moved to index */
        .sticky-footer {
            display: none;
        }

        /* Window controls */
        .window-controls {
            position: fixed;
            top: 0;
            right: 2rem; /* Match content padding */
            height: var(--title-height);
            color: var(--dos-yellow);
            display: flex;
            align-items: center;
            font-weight: bold;
            z-index: 1001;
        }

        /* DOS-style form customization */
        .formkit-form {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: none !important;
            font-family: 'DOS', monospace !important;
            padding: 1rem !important;
            margin: 0 !important;
        }
        .formkit-form * {
            font-family: 'DOS', monospace !important;
        }
        .formkit-form button,
        .formkit-form [data-element="submit"] {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
            font-family: 'DOS', monospace !important;
            text-transform: uppercase !important;
        }
        .formkit-form button:hover,
        .formkit-form [data-element="submit"]:hover {
            background: var(--dos-yellow) !important;
            color: var(--dos-black) !important;
        }
        .formkit-form input,
        .formkit-form [data-element="field"] {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
            font-family: 'DOS', monospace !important;
        }
        .formkit-form .formkit-alert {
            background: var(--dos-black) !important;
            color: var(--dos-yellow) !important;
            border: 1px solid var(--dos-yellow) !important;
        }
        .formkit-form .formkit-guarantee {
            color: var(--dos-yellow) !important;
        }
        #convertkit-form-container {
            max-width: 640px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        /* Remove decorative prompt */
        nav:before {
            content: none;
        }

        /* Mobile navigation adjustments */
        @media (max-width: 600px) {
            .content-wrapper {
                padding: 0 1rem 4rem 1rem;
            }

            nav {
                display: none;
                left: 1rem;
                right: 1rem;
                padding: 1rem;
            }

            nav.show {
                display: block;
            }

            #dos-prompt {
                padding: 1rem;
            }

            .window-controls {
                right: 1rem;
            }
        }
    </style>
    <script>
        // Ensure page loads at the top
        window.onload = function() {
            window.scrollTo(0, 0);
        }
        // Also handle navigation
        document.addEventListener('DOMContentLoaded', function() {
            window.scrollTo(0, 0);
        });

        // Toggle mobile navigation
        document.addEventListener('DOMContentLoaded', function() {
            const menuToggle = document.querySelector('.menu-toggle');
            const nav = document.querySelector('nav');
            
            menuToggle?.addEventListener('click', function(e) {
                e.preventDefault();
                nav.classList.toggle('show');
            });
        });
    </script>
</head>
<body data-page-title="{{ title }}">
    <div class="window-controls">[guest]</div>
    <div class="scanlines"></div>
    
    <nav role="navigation" aria-label="Main navigation">
        <a href="{{ baseUrl }}/blog/" aria-label="Go to Recent Posts">RECENTS</a>
        <a href="{{ baseUrl }}/ux-work/" aria-label="Go to UX Work">UX WORK</a>
        <a href="{{ baseUrl }}/about.html" aria-label="Go to About">ABOUT</a>
        <a href="{{ baseUrl }}/faq.html" aria-label="Go to FAQ">FAQ</a>
        <a href="{{ baseUrl }}/" aria-label="Go to Index">INDEX</a>
    </nav>

    <div class="content-wrapper">
        {% block content %}
        {{ content | safe }}
        {% endblock %}
    </div>

    <!-- DOS Prompt -->
    <div id="dos-prompt"></div>
    
    <footer>
        <!-- Empty footer -->
    </footer>
</body>
</html> 